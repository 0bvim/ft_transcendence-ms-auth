// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { FastifyReply, FastifyRequest } from 'fastify';

// This file is now empty because @fastify/cookie already provides its own type definitions
// The custom type declarations were causing conflicts with the official types
